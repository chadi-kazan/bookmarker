import React from 'react-native';

const { View, Text, Component } = React;

export default class Chapters extends Component {
    componentDidMount() {
        
    }
    render(): ReactElement {
        return <View><Text>Sections</Text></View>;
    }
}
