import Settings from './settings';
import Home from './home';

const Views = [
    {
        key: 'settings',
        module: Settings
    },
    {
        key: 'home',
        module: Home
    }
];

export default Views;
